# News MCP Server Configuration

[server]
name = "news-mcp"
version = "0.1.0"
host = "0.0.0.0"
port = 8080
transport_mode = "http"  # Options: stdio, http, sse, hybrid

[poller]
interval_secs = 3600  # Poll every hour
enabled = true        # Enable background polling

[cache]
max_articles_per_category = 100

[logging]
level = "info"        # Options: trace, debug, info, warn, error
enable_console = true

# Feed source overrides (uncomment to customize)
# Built-in defaults are used when not specified.
#
# [feeds.technology]
# display_name = "Technology"
# description = "Tech news from multiple sources"
# urls = [
#     "https://techcrunch.com/feed/",
#     "https://feeds.arstechnica.com/arstechnica/index",
#     "https://www.theverge.com/rss/index.xml",
# ]
# enabled = true
#
# [feeds.science]
# display_name = "Science"
# urls = ["https://www.sciencedaily.com/rss/all.xml"]
# enabled = true
#
# [feeds.instant]
# display_name = "即时新闻"
# urls = ["https://www.chinanews.com.cn/rss/scroll-news.xml"]
# enabled = true